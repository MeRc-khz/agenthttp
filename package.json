{
  "name": "agenthttp",
  "version": "1.0.0",
  "description": "An HTTP server that is itself an AI agent. Every request enters an agent loop that synthesizes context from the LLM Wiki, MCP tools, session memory, and real-time data — then renders the response as a rich HTML multimedia experience using Canvas 2D and Three.js WebGL.",
  "main": "renderer.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "express": "^5.2.1"
  }
}
